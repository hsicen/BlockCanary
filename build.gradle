buildscript {
  repositories {
    google()
    mavenCentral()
    jcenter()
  }
  dependencies {
    classpath 'com.android.tools.build:gradle:7.2.1'
  }
}

allprojects {
  repositories {
    google()
    mavenCentral()
    jcenter()
  }

  ext {
    LIBRARY_COMPILE_SDK_VERSION = 33
    LIBRARY_MIN_SDK_VERSION = 26
    LIBRARY_TARGET_SDK_VERSION = 33
  }
}
